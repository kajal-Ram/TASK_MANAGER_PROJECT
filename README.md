# TASK_MANAGER_PROJECT
A role-based Task Manager API built with Django REST Framework. Supports JWT Authentication, CRUD operations, pagination, filtering, searching, and due-date sorting. Includes unit tests and Docker support for production-ready deployment.
